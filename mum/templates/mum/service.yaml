apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-mum-service"
  namespace: {{ .Values.namespace | quote }}
  labels:
{{ include "mum.labels" . | indent 4 }}
spec:
  type: {{ .Values.mum.service.type }}
  ports:
    - port: {{ .Values.mum.service.port }}
      targetPort: {{ .Values.mum.deployment.container.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: "{{ .Release.Name }}-mum-deployment"
    app.kubernetes.io/instance: {{ .Release.Name }}
