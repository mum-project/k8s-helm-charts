apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mum.fullname" . }}-test-connection"
  labels:
{{ include "mum.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mum.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
